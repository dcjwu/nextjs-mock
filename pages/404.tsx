import { Typography } from "@mui/material"
import { NextPage } from "next"

const NotFound: NextPage = (): JSX.Element => {
   return <Typography component="p" variant="h2">Page not found</Typography>
}

export default NotFound
